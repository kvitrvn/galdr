// Package tui contains the Bubble Tea models, views and keybindings.
//
// The TUI must depend on the player interface from internal/player,
// never on a concrete audio backend. It receives commands from the user
// and forwards them to the app layer.
package tui
